/* selectors */
export const getAllPhotos = ({ gallery }) => gallery.photos;

/* action creator name */

/* action type */

/* action creator */

/* thunk creators */

/* reducer */
export const reducer = (statePart =[], action= {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
